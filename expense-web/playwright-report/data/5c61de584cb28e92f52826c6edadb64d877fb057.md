# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: merchant-list.spec.ts >> ExpenseTracking W4 capstone happy-path >> merchant list page loads and shows merchants
- Location: e2e\merchant-list.spec.ts:4:3

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('list', { name: /merchant-list/i })
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('list', { name: /merchant-list/i })

```

```yaml
- heading "Sign in" [level=1]
- button "Sign in (stub)"
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('ExpenseTracking W4 capstone happy-path', () => {
  4  |   test('merchant list page loads and shows merchants', async ({ page }) => {
  5  |     await page.addInitScript(() => {
  6  |       window.localStorage.setItem('jwt', 'dev-fake-jwt-token');
  7  |     });
  8  |     await page.goto('/merchants');
> 9  |     await expect(page.getByRole('list', { name: /merchant-list/i })).toBeVisible({ timeout: 10000 });
     |                                                                      ^ Error: expect(locator).toBeVisible() failed
  10 |     const items = page.getByRole('listitem');
  11 |     await expect(items.first()).toBeVisible();
  12 |   });
  13 | });
  14 | 
```